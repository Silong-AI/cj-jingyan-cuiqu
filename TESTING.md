# 发布前四层自测清单（Self-Test Checklist）— CJ·经验萃取 v4.1

> 对外开放前的质检。四层从"机器可验"到"第三方盲测"逐层递进：**① 回归基线 → ② 真实萃取演练 → ③ Voice Check → ④ 黑盒盲测**。全部通过再打 Release。

---

## 自测总览

| 层 | 名称 | 类型 | 谁跑 | 通过标准 |
| --- | --- | --- | --- | --- |
| ① | 回归基线 | 自动化 | 你自己 | --check PASS + --grade 10/10 |
| ② | 真实萃取演练 | 人工对话 | 你自己 + 萃取师(AI) | 挖出 ≥1 条牛招级打法 |
| ③ | Voice Check | 人工自评 | 你自己 | 6 项 ≥5 项通过 |
| ④ | 黑盒盲测 | 第三方 | 同事 / 另一个 AI | 触发 ≥9/10；SOP 可用性 ≥60 分 |

---

## 第 1 层：回归基线（自动化）

**目的**：确认 description 触发词与入口分流逻辑没被改坏。

**跑法**（在 skill 根目录）：

```bash
# ① 语料结构合法
python scripts/run_evals.py --check
# ② 生成评测模板（含期望值）
python scripts/run_evals.py --report
# ③ 用真实触发结果比对期望 → 全对写 PASS 基线
python scripts/run_evals.py --grade <actual_results.json>
```

**通过标准**：`--check` 输出 `[PASS] 语料合法：10 例`；`--grade` 输出 `[PASS]`（10/10 全对，含 T7/T8 两例不触发）。

**记录**：
- [ ] 日期：____　结果：PASS / FAIL　发现：____

---

## 第 2 层：真实萃取演练（人工）

**目的**：验证"对话式动态萃取"在真实对话里的体验，而不只是文档好看。

**跑法**（选一个真实场景，萃取对象可以是销冠/老员工/你自己）：

1. **定**：明确 场景 / 萃取对象 / 给谁用 / 档位（快速·标准·深度）——30 秒内分流确认。
2. **萃**：走至少 3 轮追问，每轮看萃取师给的"2~5 个编号追问方向菜单"：
   - 菜单是否承接上一句？方向是否有差异？
   - 有没有把"观点/结论"拉回"动作/细节"？
   - 有没有识别出"牛招"并深挖？
3. **验**：对挖出的每条打法过 V1 跨域 / V2 预测力 / V3 独特性。
4. **用**：产出至少 1 张打法卡（含一句话SOP/步骤/工具/验证），组织场景再套技能包骨架。

**通过标准**：能挖出 **≥1 条"牛招级"**打法（符合难复制/反直觉/有失败对照/有数据/差异显著）；对话自然、有金句保留。

**记录**：
- [ ] 场景：____　萃取对象：____　挖出打法数：____　其中牛招：____
- [ ] 最满意的一轮追问：____　最不满意的一轮：____

---

## 第 3 层：Voice Check（6 项自评）

**目的**：检验追问本身像不像"专业萃取师"，而不是聊天机器人。

| 检查项 | 通过标准（像萃取师） | 自评（✓/✗） |
| --- | --- | --- |
| 追问自然度 | 承接对方上一句、顺着语境追问 | [ ] |
| 对抗认知闭合 | 对方想总结时主动拉回动作层 | [ ] |
| 菜单设计 | 2~5 个编号方向，方向有差异且对应不同回答类型 | [ ] |
| 话术具体性 | "你第一步先动了哪一步？"而非"能详细说说吗" | [ ] |
| 挖掘推进 | 每轮往更深一层走（方法→牛招→心法） | [ ] |
| 金句保留 | 记录原话、原数字、原画面 | [ ] |

**通过标准**：6 项 ≥5 项 ✓；<5 项则回炉调整追问话术再测一轮。

**记录**：- [ ] 通过项：____/6　需改进：____

---

## 第 4 层：黑盒盲测（第三方）

**目的**：脱离作者视角，验证一个"陌生人"能否正确触发与使用。

**跑法 A · 触发盲测**：把 `tests/eval_set.json` 的 10 条输入（不透露答案）交给同事或另一个 AI，让它判断"这条该不该触发 CJ、分流到哪"。对比期望。

**跑法 B · SOP 可用性测试**：让一个完全没用过本 skill 的"新人"照着 demo 技能包（`examples/demo-新客开发-销冠技能包/`）里的一张打法卡做一次模拟演练，看能否达到 60 分（能做对关键动作）。

**通过标准**：触发盲测 **≥9/10** 正确；SOP 可用性 **≥60 分**。

**记录**：
- [ ] 盲测人：____　触发正确：____/10
- [ ] SOP 演练得分：____/100　卡点：____

---

## 自测结论与发布门禁

- [ ] 第 1 层通过（回归基线全绿）
- [ ] 第 2 层通过（挖出牛招级打法）
- [ ] 第 3 层通过（Voice Check ≥5/6）
- [ ] 第 4 层通过（盲测 ≥9/10 + SOP ≥60）

**发现的问题与修复记录**：
| 问题 | 影响层 | 修复 |
| --- | --- | --- |
|  |  |  |

**结论**：□ 可以发布　□ 需修复后复测（问题见上）

> 发布后打 GitHub Release tag（如 v4.1.0），release 说明附本清单结果，证明"质量靠评测卡点"。
